# Create Extension Tables

This should all be familiar from the [ReadDemo](../readdemo/create-table.mdx).

```ts copy filename="examples/WriteDemo/packages/contracts/mud.config.ts" showLineNumbers
import { defineWorld } from "@latticexyz/world";

export default defineWorld({
  namespace: "PluginExamples",
  systems: {
    WriteDemoSystem: {
      name: "WriteDemoSystem",
      openAccess: true,
    },
  },
  tables: {},
});
```

More details on MUD Config can be found [here](https://mud.dev/config).
